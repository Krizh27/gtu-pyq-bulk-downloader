# Documentation Changelog

## [2026-06-18]
- **`docs/folder-explanation.md`**: Removed references to `openapi.yaml` as the file was removed from the backend and code generation is no longer used.
- **`docs/folder-explanation.md`**: Updated backend `/api` description to clarify that Zod schemas validate against strict internal structures rather than the removed OpenAPI spec.
- **General Verification**: Verified that all file extensions across the documentation (`.jsx`, `.js`) accurately reflect the current JavaScript-only project structure. No other structural changes were required as the documentation was already mostly up-to-date with the TypeScript removal.
